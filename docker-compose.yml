version: '3.8'
services:
  dev:
    build: .
    volumes:
      - ./:/workspace:cached
    ports:
      - "3000:3000"
      - "35729:35729"
    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped
